
pipeline:

  build:
    image: golang:1.8
    commands:
      - go install
      - go build                
      
      
  gcr:
    image: plugins/docker
    registry: gcr.io
    repo: gcr.io/zang-api-sandbox/godrone
    privileged: true
    secrets: [ docker_username, docker_password ]
    debug: true
    tag: "2.0"
